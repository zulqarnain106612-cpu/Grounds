<!-- AUTO-GENERATED by gateway/docgen.py from schema/agent.schema.json. Do not hand-edit. -->
# JetFighter Agent Schema Reference

Schema version: `1.1.0`

Every request/response is one JSON object validated against `schema/agent.schema.json`. `additionalProperties: false` is set on every object in this schema, so any unknown field is a hard rejection, not a warning.

## Top-level shape

| section | required | purpose |
|---|---|---|
| `meta` | yes | session/versioning envelope |
| `intent` | yes | what action, on what domain, at what priority |
| `payload` | yes | the action-specific data |
| `response` | no (server-filled) | filled in by `gateway/gateway.py`, itself schema-validated before being returned |

## intent.action (enum)

- `write` — handled
- `build` — handled
- `delete` — handled
- `query` — handled
- `index` — handled
- `retrieve` — handled
- `tool_call` — handled
- `command_exec` — handled
- `daemon_start` — handled
- `daemon_stop` — handled
- `daemon_status` — handled
- `log_query` — handled
- `symbol_lookup` — handled
- `schema_validate` — handled
- `knowledge_update` — handled
- `context_push` — handled
- `context_pop` — handled
- `ci_dispatch` — handled
- `ci_status` — handled
- `ci_logs` — handled
- `ingest` — handled
- `manifest` — handled
- `test_run` — handled

## intent.domain (enum)

- `player`
- `enemy`
- `weapon`
- `physics`
- `network`
- `ui`
- `audio`
- `asset`
- `scene`
- `build_pipeline`
- `knowledge_base`
- `retrieval`
- `daemon`
- `log`
- `schema`
- `ci`
- `qa`

## payload sub-objects

### RetrievalContext

| field | type | required | notes |
|---|---|---|---|
| `strategy` | string | yes | enum: exact, semantic, keyword, hybrid, symbol, log_failure |
| `scope` | string | no | enum: file, directory, kb, symbol_table, log |
| `target` | string | no |  |
| `line_range` | array | no |  |
| `max_tokens` | integer | no |  |
| `top_k` | integer | no |  |
| `filter` | object | no |  |

### CommandExec

| field | type | required | notes |
|---|---|---|---|
| `cmd` | string | yes |  |
| `args` | array | no |  |
| `capped` | boolean | yes | const: True |
| `timeout_ms` | integer | no |  |
| `working_dir` | string | no |  |
| `env` | object | no |  |
| `output_lines` | integer | no |  |

### ToolCall

| field | type | required | notes |
|---|---|---|---|
| `tool_name` | string | yes |  |
| `parameters` | object | no |  |
| `capped` | boolean | yes | const: True |
| `max_output` | integer | no |  |

### FileOp

| field | type | required | notes |
|---|---|---|---|
| `op` | string | yes | enum: write, append, delete, stat |
| `path` | string | yes |  |
| `line_start` | integer | no |  |
| `line_end` | integer | no |  |
| `content` | string | no |  |
| `encoding` | string | no |  |

### LogOp

| field | type | required | notes |
|---|---|---|---|
| `op` | string | yes | enum: write, query_failure, tail, stat |
| `level` | string | no | enum: debug, info, warn, error, fatal |
| `message` | string | no |  |
| `domain` | string | no |  |
| `tail_lines` | integer | no |  |
| `filter_level` | string | no | enum: warn, error, fatal |

### KnowledgeNode

| field | type | required | notes |
|---|---|---|---|
| `op` | string | yes | enum: add_node, update_node, add_edge, remove_node |
| `id` | string | yes |  |
| `type` | string | no |  |
| `label` | string | no |  |
| `tags` | array | no |  |
| `phase` | array | no |  |
| `symbols` | array | no |  |
| `edge_to` | string | no |  |
| `edge_kind` | string | no |  |

### DaemonOp

| field | type | required | notes |
|---|---|---|---|
| `daemon_id` | string | yes |  |

### AgentError

| field | type | required | notes |
|---|---|---|---|
| `code` | string | yes |  |
| `message` | string | yes |  |
| `domain` | string | no |  |
| `fatal` | boolean | no |  |

### Symbol

| field | type | required | notes |
|---|---|---|---|
| `name` | string | yes |  |
| `kind` | string | yes | enum: class, method, field, interface, enum, const, struct, prefab, scene, asset |
| `file` | string | yes |  |
| `line` | integer | yes |  |
| `namespace` | string | no |  |
| `tags` | array | no |  |

### RetrievedChunk

| field | type | required | notes |
|---|---|---|---|
| `source` | string | yes |  |
| `score` | number | yes |  |
| `content` | string | yes |  |
| `line_start` | integer | no |  |
| `line_end` | integer | no |  |
| `strategy` | string | no |  |

### CIOp

| field | type | required | notes |
|---|---|---|---|
| `op` | string | yes | enum: dispatch, status, logs |
| `pr` | integer | no | Pull request number. Required when op is 'status' or 'logs' -- a repo-wide status or log request is not expressible. |
| `workflow` | string | no | Workflow file name under .github/workflows, e.g. 'ingest.yml'. |
| `ref` | string | no |  |
| `inputs` | object | no |  |
| `run_id` | integer | no |  |
| `limit` | integer | no |  |
| `log_tail_lines` | integer | no | Exact number of log lines to return. No default: omitting it returns the fixed 2-line error probe instead. Set it only to a number the probe justified. |
| `log_offset` | integer | no | Lines to skip from the end before the window starts, for when the probe shows the cause sits above the tail. |
| `remote_only` | boolean | yes | Must be true. Asserts the work runs on GitHub Actions, never on the local machine. const: True |
| `wait` | boolean | no | Must be false. Dispatch returns immediately; callers never block on a run. const: False |

### TestOp

| field | type | required | notes |
|---|---|---|---|
| `suite` | string | yes | Which verification technique to run. 'editmode'/'playmode' are the two Unity Test Framework modes; the rest are Python-side suites. enum: unit, regression, property, metamorphic, mutation, contract, soak, performance, editmode, playmode, all |
| `scope` | string | no | Optional selector passed to the runner (a pytest -k expression, or an NUnit test filter). Capped so it cannot become a command line. |
| `min_tests` | integer | no | Fail the run unless at least this many tests actually executed. The minimum is 1, not 0, on purpose: both pytest and Unity's runner exit 0 when they discover nothing, so a request that tolerates zero tests is a request for a green lie. It is not expressible here. |
| `seed` | integer | no | PRNG seed for randomised suites. Required for 'property' and 'soak' (see allOf): a randomised failure nobody can replay is an anecdote, not a regression. |
| `coverage_min` | number | no | Line+branch coverage floor, percent. Defaults to the repo gate when omitted. |
| `mutation_score_min` | number | no | Percent of injected mutants the suite must kill. Required for the 'mutation' suite -- a mutation report with no floor is a number nobody is obliged to act on. |
| `budget_ms` | number | no | Wall-clock ceiling for the 'performance' suite, which requires it. A timing run with no budget produces a measurement, not a verdict. |
| `baseline` | string | no | Identifier of the golden artefact to compare against. Required for the 'regression' suite, which has nothing to regress against without it. |
| `remote_only` | boolean | yes | Must be true. execution_policy forbids running tests on the local machine; this asserts the run happens on GitHub Actions. const: True |
| `wait` | boolean | yes | Must be false. Dispatch returns immediately; callers never block on a run. const: False |

## Enforcement guarantees encoded in this schema

- `FileOp.op` enum: `['write', 'append', 'delete', 'stat']` — notice there is no `read`. There is no schema-valid way to ask this gateway for raw file content.
- `RetrievedChunk.content` has `maxLength: 2000`.
- `CommandExec.capped` is `const: true` — a request that sets it to `false` fails validation before anything runs.
- `LogOp.tail_lines` maxes at `20`.

See `docs/ENFORCEMENT.md` for how these constraints are enforced in code, not just declared.
